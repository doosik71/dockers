# DEVELOPMENT.md

## 1. 프로젝트 개요

`dockers`는 Docker를 텍스트 콘솔 환경에서 쉽게 사용할 수 있도록 만드는 Rust 기반 메뉴형 도구이다. 목표는 Docker CLI의 강력함은 유지하되, 메뉴 탐색, 상태 요약, 확인 절차를 통해 사용성을 크게 높이는 것이다.

## 2. 핵심 개발 원칙

- 초보자 친화적일 것
- 콘솔 환경에서 빠르고 안정적으로 동작할 것
- Windows와 Ubuntu에서 동일한 사용 경험을 제공할 것
- 파괴적 작업에 대해 안전 장치를 제공할 것
- 내부 구조는 확장 가능하게 설계할 것

## 3. 제품 요구사항

### 기능 요구사항

- Docker 엔진 연결 상태를 확인할 수 있어야 한다.
- 컨테이너, 이미지, 볼륨, 네트워크 목록을 조회할 수 있어야 한다.
- 각 리소스에 대해 기본 관리 작업을 수행할 수 있어야 한다.
- 사용자는 메뉴와 단축키로 탐색할 수 있어야 한다.
- 실행 전 작업 대상과 작업 결과를 명확히 보여줘야 한다.
- 실패 시 에러 메시지를 기술적 원인과 사용자 행동 기준으로 함께 안내해야 한다.

### 비기능 요구사항

- 콘솔 창 크기가 제한적이어도 동작해야 한다.
- 느린 Docker 응답에서도 UI가 가능한 한 멈추지 않아야 한다.
- 외부 의존성을 과도하게 늘리지 않아야 한다.
- 테스트 가능한 구조를 가져야 한다.
- 운영체제별 차이를 추상화해야 한다.

## 4. 아키텍처 초안

초기 버전은 Docker Engine API 직접 연동보다 `docker` CLI 호출 방식을 우선 채택한다.

이유는 다음과 같다.

- 빠른 프로토타이핑이 가능하다.
- 사용자의 기존 Docker 설치 환경을 그대로 활용할 수 있다.
- Windows와 Ubuntu 모두에서 동일한 접근이 비교적 쉽다.
- 초기에 필요한 기능 대부분은 `docker` CLI 결과 파싱으로 구현 가능하다.

향후 구조는 아래와 같이 분리한다.

- `ui`
  - 메뉴, 목록, 입력, 확인 창, 상태 표시
- `application`
  - 사용자 흐름, 화면 전환, 작업 orchestration
- `domain`
  - 컨테이너, 이미지, 볼륨, 네트워크 모델
- `infrastructure`
  - Docker CLI 실행, 결과 파싱, OS 차이 처리
- `common`
  - 에러, 설정, 로깅, 유틸리티

## 5. 기술 설계 방향

### UI 방식

후보는 다음 두 가지이다.

- 단순 메뉴형 CLI
- TUI 기반 인터페이스

권장 방향은 TUI 우선 검토이다. 이유는 한 화면에서 목록, 상태, 도움말, 액션을 함께 보여주기 쉽기 때문이다. 다만 초기 버전은 구현 복잡도를 낮추기 위해 최소한의 TUI 또는 단순 메뉴형 CLI로 시작할 수 있다.

후보 라이브러리 예시:

- `ratatui`
- `crossterm`
- `inquire` 또는 유사 입력 보조 라이브러리

### Docker 연동

초기 단계:

- `docker version`
- `docker info`
- `docker ps`
- `docker images`
- `docker volume ls`
- `docker network ls`

명령 결과는 가능하면 표준 출력 포맷 옵션을 활용해 파싱 부담을 줄인다.

예:

- `--format` 옵션 사용
- JSON 출력 가능 여부 검토

### 오류 처리

오류는 다음 범주로 분류한다.

- Docker 미설치
- Docker daemon 미실행
- 권한 부족
- 대상 리소스 없음
- 명령 실행 실패
- 출력 파싱 실패

각 오류는 내부 에러와 사용자 메시지를 분리해 관리한다.

## 6. 단계별 개발 계획

### Phase 0. 기획 및 설계

목표:

- 문제 정의와 범위를 문서화한다.
- MVP 기능을 확정한다.
- 초기 아키텍처를 결정한다.

산출물:

- `README.md`
- `DEVELOPMENT.md`
- `TODO.md`

### Phase 1. 프로젝트 골격 구성

목표:

- Rust 프로젝트를 초기화한다.
- 기본 모듈 구조와 실행 진입점을 만든다.
- 설정, 에러, 프로세스 실행 공통부를 준비한다.

주요 작업:

- Cargo 프로젝트 생성
- 모듈 디렉터리 구성
- 에러 타입 설계
- Docker CLI 실행 래퍼 작성

### Phase 2. Docker 연결 및 조회 기능

목표:

- Docker 연결 상태와 주요 리소스 목록을 조회할 수 있게 한다.

주요 작업:

- Docker 설치 및 daemon 상태 확인
- 컨테이너 목록 조회
- 이미지 목록 조회
- 볼륨 목록 조회
- 네트워크 목록 조회

완료 기준:

- 콘솔에서 목록이 정상 표시된다.
- Docker 미설치 또는 미실행 상태를 구분해 안내한다.

### Phase 3. 컨테이너 기본 관리

목표:

- 사용자가 가장 자주 수행하는 컨테이너 작업을 지원한다.

주요 작업:

- 컨테이너 시작
- 컨테이너 중지
- 컨테이너 재시작
- 컨테이너 삭제
- 로그 조회
- 상세 정보 조회
- 쉘 진입

완료 기준:

- 선택한 컨테이너에 대해 주요 작업이 메뉴에서 수행된다.
- 파괴적 작업 전 확인 절차가 제공된다.

### Phase 4. 이미지/볼륨/네트워크 관리

목표:

- 컨테이너 외 리소스도 일관된 방식으로 관리한다.

주요 작업:

- 이미지 삭제
- 볼륨 삭제
- 네트워크 삭제
- 리소스 상세 정보 조회

완료 기준:

- 리소스별 목록, 선택, 작업, 결과 표시 흐름이 통일된다.

### Phase 5. 사용성 강화

목표:

- 탐색성과 반복 작업 효율을 높인다.

주요 작업:

- 검색
- 필터
- 정렬
- 다중 선택
- 단축키 도움말
- 상태바/피드백 메시지 개선

### Phase 6. 생성 흐름 지원

목표:

- 초보자도 새 컨테이너를 만들 수 있도록 생성 마법사를 제공한다.

주요 작업:

- 이미지 선택 또는 입력
- 컨테이너 이름 지정
- 포트 매핑 입력
- 볼륨 마운트 입력
- 환경 변수 입력
- 실행 전 요약 확인

### Phase 7. 품질 및 배포

목표:

- 테스트, 문서, 빌드, 릴리스 절차를 정리한다.

주요 작업:

- 단위 테스트
- 통합 테스트
- 사용자 시나리오 점검
- Windows/Ubuntu 빌드 검증
- 배포 문서 작성

## 7. 테스트 전략

### 단위 테스트

- 출력 파서
- 상태 변환 로직
- 입력 검증 로직
- 에러 매핑 로직

### 통합 테스트

- Docker CLI 래퍼 동작
- 주요 명령 실행 결과 처리
- 오류 상황 처리

### 수동 테스트

- Docker가 없는 환경
- Docker daemon이 중지된 환경
- 컨테이너가 없는 환경
- 리소스가 많은 환경
- 작은 터미널 크기 환경
- Windows PowerShell / Ubuntu bash 환경

## 8. 리스크와 대응

- Docker CLI 출력 형식 차이
  - `--format` 사용을 우선 검토한다.
- 운영체제별 쉘 차이
  - 프로세스 실행 계층을 추상화한다.
- TUI 라이브러리 복잡도
  - 초기 버전은 단순 구조로 제한한다.
- 쉘 진입 기능의 플랫폼 차이
  - 기능을 실험적으로 도입하고 단계적으로 안정화한다.
- 파괴적 명령의 실수 가능성
  - 확인 창, 대상 요약, 취소 기본값을 적용한다.

## 9. 향후 의사결정이 필요한 항목

- 초기 UI를 TUI로 바로 시작할지, 단순 메뉴형 CLI로 시작할지
- Docker CLI 기반을 언제까지 유지할지
- 설정 파일 포맷을 무엇으로 할지
- 로그/감사 기록 저장 범위를 어디까지 둘지
- Compose 지원을 MVP 이후 언제 포함할지

## 10. 현재 가정

현재 계획은 다음 가정을 기반으로 작성되었다.

- 대상 사용자는 Docker 초중급 사용자이다.
- 우선순위는 사용성 개선이며, Docker 기능 완전성은 후순위이다.
- MVP는 조회와 기본 관리 기능 중심이다.
- 로컬 Docker 환경과 원격 터미널 환경 모두를 고려한다.
